require('../../helpers/runner');

var namespace = require('../../../../www/app/helpers/namespace');

describe('snug test suite', function () {

  it('should have basic tests', function () {
    expect(true).to.be.ok();
  });

});
